# Who's behind this project

Franco (git identity "Franco", email dec1503409765@gmail.com) drives `opencode-toolkit` (renamed from `opencode-qwen-prompt` 2026-09-19) solo, across multiple machines/environments (this Mac, a Windows target machine, possibly more) — not a team, so nothing here should be framed as "collaboration with other people." Comfortable with Docker, git, and shell scripting; reads and writes fluently in both Chinese (chat) and English (this repo's actual content, per a personal punctuation-language convention: English punctuation even inside Chinese text).

Technical, doesn't need hand-holding — skip explaining what standard tools/commands do. Notices structural rot quickly: duplicated config values, stale cross-references, flat/disorganized directory layouts, prose that's more verbose than it needs to be.

Working preferences for this project (deliver confirmations as files rather than long chat replies, verify claims against the real system before asserting them, centralize config values instead of duplicating literals, don't hard-wrap prose since the editor in use already soft-wraps) are recorded in `CLAUDE.md`'s "Preferences" section, not repeated here.
