# Project memory

`memory/` is git-tracked cross-machine memory about who's behind this project and its context, separate from CLAUDE.md's "how to work on it" focus.

**Not applicable for testing** — it's documentation about the project, not application behavior.
