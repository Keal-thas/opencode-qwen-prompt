# TODO

Concrete, actionable follow-up work for this repo. Different from README.md's "Status / open items" (known limitations/facts about the current setup) — this file is things that should actually get done. Remove an item once it's done instead of leaving it checked off.

- **Oracle/Loki singleton first-time-startup race (deferred).** If two worktrees both race to bring up a shared Oracle/Loki container for the very first time ever on a machine (`docker/dev.sh` does this idempotently before every `opencode-dev` invocation — see `docker/docker-compose.oracle.yml`/`docker/docker-compose.loki.yml`), one `docker compose up -d` succeeds and the other gets a real, non-zero-exit `Conflict: container name already in use` error. Once the container is already running, later concurrent `up -d` calls from any worktree are fine (idempotent reconcile). Narrow window, low blast radius (just retry) — a `flock`-style lock around "ensure Oracle/Loki are up" in `docker/dev.sh` would close it if it ever actually bites.

- **Docker image tag (`opencode-qwen-prompt-dev:latest`) build race across worktrees (deferred).** The image is deliberately global/shared across all worktrees (rebuilding a multi-GB toolchain image per worktree would be wasteful) — so it sits outside the per-worktree isolation `docker/dev.sh` gives everything else. If two worktrees have different uncommitted `Dockerfile` content and both run `docker/dev.sh build` around the same time, whichever finishes last silently wins the `:latest` tag — the other's successful build just becomes untagged/dangling, and that worktree's next `docker/dev.sh run` quietly uses someone else's image instead of the one it just built, with no error.

- **Verify the whole-repo npm publish end-to-end (added 2026-09-18).** `package.json` + `scripts/publish-npm.sh` publish the repo as `@kealthas-dev/opencode-qwen-prompt` on public npmjs.com so the restricted machine can `npm pack` it instead of a manual zip transfer (see CLAUDE.md's "Whole-repo npm publish" entry). Needs confirming: (1) the restricted machine's internal npm mirror actually resolves and serves `@kealthas-dev/...` packages (it mirrors public npm for downloads only, per CLAUDE.md, but scoped packages specifically haven't been checked), (2) `npm pack` + extract on that machine reproduces the same file tree as the current zip transfer, including `docs/opencode-docs-reference/` and `deploy/models-dev-snapshot.json`.

- **Verify the automated npm publish actually works end-to-end (added 2026-09-18).** `release.yml` now publishes to npm via Trusted Publishing (OIDC, no stored token — see CLAUDE.md) whenever a `v*` tag is pushed, using `npm pkg set version="${GITHUB_REF_NAME#v}"` to derive the published version from the tag. This has never actually run — this workflow change and the matching npmjs.com Trusted Publisher config (org `Keal-thas`, repo `opencode-qwen-prompt`, workflow filename `release.yml`, direct `npm publish` explicitly allowed) both need to reach `master` before a real tag push will exercise it. Push a real `vX.Y.Z` tag after merging and confirm the Action's "Publish to npm" step actually succeeds (npm CLI version on the runner, OIDC token exchange, etc. are all unverified in practice, only from docs).
