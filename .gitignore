.claude/
*.log
.DS_Store
Thumbs.db
node_modules/

# Scratch/handoff notes, local experiment output, etc. - never meant to be
# committed, kept out of the repo root so they don't show up as stray
# untracked files.
.local/

# Local-only copy of opencode's own docs (packages/web/src/content/docs/*.mdx
# from anomalyco/opencode), for reference while working on this repo. Third-
# party content, not part of this repo's actual deliverable - not tracked.
opencode-docs-reference/
.idea/
